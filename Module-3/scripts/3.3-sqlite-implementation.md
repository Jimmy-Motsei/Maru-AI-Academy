# 3.3 — Implementing SQLite (Instructor Script)

**Length:** 10–12 min

- Use `better-sqlite3` for sync, simple queries.
- Dependency injection in routes for testability.
- Per-test temp DB file; truncate between tests.
